Reset menu

Press and hold to access the reset choices presented in this menu.

Reset brush size        Reset the brush to its default size.
Reset point colors      Restore all points to the default color.
Reset line colors       Restore all lines to the default color.
Reset glyphs            Restore all points to the default glyph.

