Send Tour Update

If a second XGobi is initiated and you wish to send the coefficients
from the current one to it, then click on Send Tour Update. This
causes the second window to show the same tour projection as the
first.

