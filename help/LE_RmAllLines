Remove all lines

Click here to remove all connecting lines, presumably because
you wish to read in a new file or interactively create a new
set of lines.

Note:  To simply not show lines without removing them,
deactivate the "Show lines" button instead.
